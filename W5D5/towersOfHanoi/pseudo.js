class Game {
  constructor() {
    // until won? do
    //  show the board (render)
    //  prompt the player for a move
    //  check if the move is valid...
    //  and if valid make the move
  }
}
